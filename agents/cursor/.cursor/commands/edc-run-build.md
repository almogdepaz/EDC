---
description: Builds or updates deep architectural context for the codebase
---

# Build Context

**Arguments:** optional `--force` to rebuild from scratch, `--focus <module>` for specific module, repeatable `--ignore <glob>` to exclude files for this run.

Read and follow the `edc-build-impl` skill. Pass through any arguments from the user.
