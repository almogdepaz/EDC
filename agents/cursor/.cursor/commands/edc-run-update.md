---
description: Incrementally updates .context/ files based on branch changes
---

# Update Context

**Arguments:** optional `--base <ref>` for comparison reference (default: auto-detect merge base with main/master).

Read and follow the `edc-update-impl` skill. Pass through any arguments from the user.
