---
name: edc:edc-doctor
description: Validate the v2 context tree, manifest schema, and routing coverage
---

Run:

```bash
bash plugins/edc/scripts/edc-doctor.sh
```

If it fails, surface the stderr report verbatim and stop.
